/* 변수
다양한 값을 담을 수 있는 박스와 같은 컨테이너
값이 저장된 메모리 주소와 이름을 연결, 그 주소의 별칭
*/

// 선언
let lang;

// 할당
lang = "JS";

// 재할당
lang = "JavaScript";

// 선언과 동시에 할당
let lang2 = "JavaScript";

// 재할당
lang2 = "JS";

let count = 0;

// 복합 할당 연산자 (예측하기 어려움)
count += 1;

// 잘 읽히고 예상하기 쉬움
count = count + 1;
